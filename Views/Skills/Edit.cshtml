@model CeeveeSoftWebProj.Models.Skill

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Skill</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit" asp-route-id="@Model.PortfolioId">
            
            <div class="form-group">
                <label asp-for="SkillContent" class="control-label"></label>
                <input asp-for="SkillContent" class="form-control" />
                <span asp-validation-for="SkillContent" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
